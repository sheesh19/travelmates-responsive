<!-- Renders if the user is the owner -->

<div class="event-side-title d-flex">
  <!-- Name title -->
  <div class="d-flex">
    <!-- Organiser avatar -->
    <div class="mr-3">
      <%= image_tag @event.itinerary.user.avatar, class: "avatar" %>
    </div>
    <div>
      <!-- Organiser Name -->
      <p id="event-join-organiser-label" class="mb-0">Event Status:</p>
      <%= render "shared/components/event_status_pill", event: @event %>
    </div>
  </div>

</div>
